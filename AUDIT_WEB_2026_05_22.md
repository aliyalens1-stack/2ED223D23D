# 🔍 WEB AUDIT — `/app/admin` + `/app/web-app`

**Date:** 2026-05-22
**Scope:** оба SPA — каждая `.tsx`, `package.json`, `vite.config.ts`, `App.tsx`, `services/api.ts`, реальный build, реальный TypeScript-чек, реальная coverage по 637-endpoint backend
**Method:** install → build → `tsc --noEmit` → curl против live backend → grep на orphan-files / dead imports
**Verdict:** SPA **СОБИРАЮТСЯ**, но имеют существенную **API-coupling degradation** после миграции NestJS→FastAPI

---

## 0. TL;DR

|  | Admin SPA | Web-app SPA |
|---|---|---|
| **Build** | ✅ **PASS** (3184 modules, 5.32 s, 2.0 MB) | ✅ **PASS** (6187 modules, 6.10 s, 1.8 MB) |
| **Chunks** | 15 (1 warning: `admin-ops` 524 kB > 500 kB threshold — canary specified) | 19 (zero warnings) |
| **TS errors (tsc --noEmit)** | ~22 (20 cosmetic + 2 real) | ~17 (~6 type-blind spots + ~11 real) |
| **API coverage vs backend** | 🔴 **77 / 216 endpoints OK (36%)** — **139 missing (64%)** | 🟡 **83 / 120 OK (69%)** — **37 missing (31%)** |
| **Orphan files** | 1 (intentional) | 2 |
| **Supervisor present** | ❌ not in `conf.d/` | ❌ not in `conf.d/` |
| **`dist/` собран** | ✅ только что собрал | ✅ только что собрал |

### 🔴 Главные обрывы

1. **Admin: ~50% страниц 404 в runtime**, потому что вызывают NestJS-роуты, которые так и не переехали во FastAPI после `NESTJS_ENABLED=0`. Самые большие пострадавшие:
   - **Вся Automation Suite** (15 страниц): dashboard, rules, chains, engine, ROI, shadow, idempotency, replay, failsafe, feedback, dry-run, performance, unified-state
   - Users, Organizations, Bookings, Reviews, Feature Flags, Integrations
   - Demand engine (metrics/heatmap/hot-areas/surge)
   - Market rules, Experiments, Audit log, Quotes admin

2. **Web-app: канонические "my"-пути в `shared/api-contracts.ts` указывают в пустоту:** `/quotes/my`, `/bookings/my`, `/reviews/my`, `/vehicles/my`. Backend перенёс их под `/customer/*`, `/marketplace/*`, `/trust/*` — api-contracts не обновили.

3. **Web-app: 3 реальных контракт-обрыва типов:** `InspectionJobViewModel` импортится не из того файла; `RawVehicleDoc.location` отсутствует; `InspectorExposure` потерял поля `jobId/inspectorId/createdAt`.

4. **Admin: `SupportChatPage.tsx:469` ссылается на несуществующий `AdminVoiceView`** — компонент удалили, импорт не вычистили. Страница падает при открытии (runtime ReferenceError).

---

## 1. Admin SPA — детальный разбор

### 1.1. Структура
```
/app/admin/
├── src/
│   ├── App.tsx              ~250 LOC — 86 routes, 84 lazy imports, 5 manualChunks
│   ├── main.tsx             entry с basename='/api/admin-panel'
│   ├── components/          Layout, GlobalSearchModal, NotificationBell, QuickActionsPanel
│   ├── hooks/               useNotifications, useRealtime, useRealtimeSocket
│   ├── lib/socket.ts        socket.io-client
│   ├── pages/               86 .tsx
│   ├── services/
│   │   ├── api.ts           543 LOC — 200+ endpoints axios wrapper
│   │   └── realtime.ts      WS/realtime helper
│   ├── shared/
│   │   └── api-contracts.ts canonical paths
│   └── stores/authStore.ts  Zustand
├── vite.config.ts           manualChunks: vendor-* + admin-ops/automation/governance/revenue/forecast
└── package.json             ⚠️ `cra-template` + `react-scripts` deps leftover (Vite-only сейчас)
```

### 1.2. Build status
```
✓ 3184 modules transformed.
✓ built in 5.32s
dist/assets/admin-ops-CnEty_5M.js         523.67 kB │ gzip: 106.08 kB
(!) Some chunks are larger than 500 kB after minification
```
**One warning** на `admin-ops` chunk — намеренно не повышен `chunkSizeWarningLimit` (canary для следующего refactor). 

### 1.3. TypeScript errors (22)

| Серьёзность | Файл | Ошибка | Действие |
|---|---|---|---|
| 🔴 RUNTIME | `pages/SupportChatPage.tsx:469` | `Cannot find name 'AdminVoiceView'` — компонент удалён, JSX-ссылка осталась | удалить блок или восстановить компонент |
| 🟡 API DRIFT | `pages/ReviewsPage.tsx:42` | `Expected 2 arguments, but got 1` — сигнатура функции изменилась | проверить вызов |
| 🟢 NOISE (×19) | различные | TS6133 — declared but never read (unused imports lucide-icons, неиспользуемые setters) | `tsc` noise; auto-cleanup |
| 🟢 NOISE | `services/realtime.ts:18` | unused `token` parameter | cosmetic |
| 🟢 NOISE | `shared/domain/parsers/__tests__/canonical.test.ts` | `vitest` не установлен в admin — это файл из `/app/shared`, видим через path alias | shared-pollution, не критично |

### 1.4. Orphan files
**1 файл:**
- `pages/NotificationsPageLegacy.tsx` — **намеренно сохранён**, упоминается в комментарии `NotificationsPage.tsx:15` ("preserved for ops continuity"). Не баг.

Все остальные 85 страниц замаппены в `App.tsx` (`lazy(() => import(...))`) и имеют `<Route>`.

### 1.5. 🔴 API coupling — главная находка

Скан всех `api.METHOD(...)` calls в `services/api.ts` + `shared/api-contracts.ts`, сравнение с live `/openapi.json`:

```
Admin total unique path patterns: 216
  ✅ Resolved on backend:  77  (36%)
  ❌ Missing on backend:  139  (64%)
```

**Подтверждённый live runtime test** (curl с admin JWT):
```
404 /api/admin/users
404 /api/admin/organizations
404 /api/admin/bookings
404 /api/admin/reviews
404 /api/admin/feature-flags
404 /api/admin/integrations
404 /api/admin/automation/dashboard
404 /api/admin/automation/rules
404 /api/admin/zones
404 /api/admin/map/zones
404 /api/admin/audit-log
404 /api/admin/notifications/templates
404 /api/admin/quality/auto-rules
404 /api/admin/quotes/all
```

**Корневая причина:** Эти endpoints были в NestJS-бэкенде (`backend/src/`), который сейчас `NESTJS_ENABLED=0` (dead code per `AUDIT_E2`). Sprint 21 FINAL вынес **только 37 endpoints** в `app/governance/router.py`. Остальные ~139 admin-endpoints остались сиротами.

### 1.6. Сводка по статусам страниц (86)

| Категория | Кол-во | Состояние |
|---|---:|---|
| ✅ **Работают** — endpoints мапятся в FastAPI | ~30 | Dashboard, LoginPage, StripePaymentsPage, StripeSettingsPage, PaymentsPage, DisputesPage, GovernanceScorePage, SystemHealthPage, SystemErrorsPage, VerificationQueuePage, ServiceMarketplaceExchange/Map, AutoRequestsPage, CarSelectionPage, IntegrationsPage*, NotificationsPage*, SettingsPage*, AdminReputationPage*, AdminInboxPage*, AdminOpsMapPage*, AdminAssignmentsPage*, ProviderInboxPage*, GeoOpsPage*, ZoneControlPage* (* — частично) |
| 🟡 **Работают частично** — некоторые API → 404, страница рендерится с ошибками | ~15 | LiveMonitorPage, ProvidersPage, ProviderDetailPage, ProviderLifecyclePage, OrganizationsPage, OperatorPerformancePage, ReputationPage, SupplyQualityPage, ReviewsPage, ServicesPage, RevenueDashboardPage, RevenueExperimentsPage, ForecastDashboardPage, IncidentControlPage, BookingsPage |
| 🔴 **Полностью сломаны (большинство API → 404)** | ~41 | **Automation suite (15):** AutomationDashboard/ControlPage, AutoActions, ActionChains, ExecutionMonitor, ExecutionReplay, ShadowMode, Idempotency, ROITracking, UnifiedState, Failsafe, FeedbackLoop, DryRun, AutoRulePerformance, RuleVisualizer.<br>**Governance (8):** ProviderBehavior, MarketControl, DemandControl, DemandActions, DistributionControl, EconomyControl, RequestFlow, SupplyQuality.<br>**Admin (10):** UsersPage, CustomersPage, ReportsPage, AuditLogPage, FeatureFlagsPage, MonetizationPage, SuggestionsPage, WorkersPage, PaymentsAndCreditsPage, CustomerNotifyPreview, CustomerNotifyLifecycle.<br>**Прочее (8):** QuotesPage, MapPage, SimulationPage, PlaybooksPage, InspectionForensicsPage, SupportChatPage (+ ReferenceError), CustomersPage |

### 1.7. Package.json housekeeping
```json
"react-scripts": "5.0.1",     ← leftover (Vite migration), 200 MB зря
"cra-template": "1.2.0",       ← leftover
"@craco/craco": "^7.1.0",      ← leftover (devDep)
"@babel/plugin-proposal-private-property-in-object": "^7.21.11"  ← leftover
```

---

## 2. Web-app SPA — детальный разбор

### 2.1. Структура
```
/app/web-app/
├── src/
│   ├── App.tsx              ~275 LOC — 3 shells (PublicShell/CustomerShell/OperatorShell)
│   │                                  + 4-уровневая защита через RequireKind
│   ├── main.tsx             entry + assertSharedDomainInvariants() в DEV
│   ├── shells/              PublicShell, CustomerShell, OperatorShell
│   ├── layouts/             InspectorCabinetShell
│   ├── pages/
│   │   ├── auth/            Login, Register
│   │   ├── public/          15 страниц (Home, Inspect, Selection, Comparison, Reports,
│   │   │                                  Specialists, Vehicle, Feed, Provider, Booking,
│   │   │                                  Region, Pricing, Cases, Packages)
│   │   ├── customer/        17 страниц
│   │   ├── provider/        10 страниц
│   │   ├── inspector/       12 страниц
│   │   ├── chat/            ChatList, ChatThread
│   │   └── notifications/   NotificationsPage
│   ├── components/          inspector/marketplace/ui
│   ├── services/
│   │   ├── api.ts           248 LOC — 14 typed API namespaces
│   │   └── realtime.ts
│   ├── shared/
│   │   └── api-contracts.ts canonical paths
│   ├── stores/authStore.ts  Zustand с activeAccount + kind
│   ├── i18n/                react-i18next setup
│   └── data/                static
├── vite.config.ts           manualChunks: vendor-* + web-customer/provider/inspector/auth/public
└── package.json             clean (no CRA leftovers)
```

### 2.2. Build status
```
✓ 6187 modules transformed.
✓ built in 6.10s
✓ zero warnings
```
**Лучше admin'а** — никаких превышений chunkSizeWarningLimit, и package.json чистый.

### 2.3. TypeScript errors (17)

| Серьёзность | Файл | Ошибка | Природа |
|---|---|---|---|
| 🔴 **CONTRACT BREAK** | `components/inspector/StatusToolbar.tsx:4` | `Module '@platform/domain/contracts/inspection-job' has no exported member 'InspectionJobViewModel'` | Импорт не из того файла. На самом деле в `shared/domain/**state-machines**/inspection-job.ts:160`. |
| 🔴 **CONTRACT BREAK** | `pages/customer/CustomerVehicleDetail.tsx:367` | `Property 'location' does not exist on type 'RawVehicleDoc'` | Поле удалено из контракта, страница ещё читает. |
| 🔴 **CONTRACT BREAK** | `pages/inspector/InspectorWorkspace.tsx:71` | `InspectorExposure` missing `jobId/inspectorId/createdAt` — UI делает unsafe cast | Контракт ушёл вперёд. |
| 🟡 **UI DRIFT** | `pages/auth/LoginPage.tsx:251,256` | `Property 'badge' does not exist on demoAccount` (×2) | Поле удалили из конфига demo-аккаунтов. |
| 🟡 **UI DRIFT** | `components/inspector/report/ReportEditor.tsx:40` | `'critical' does not exist in Record<IssueSeverity, string>` | Severity enum сузили, UI ещё пытается ссылаться. |
| 🟡 **DU NARROWING** | `pages/customer/CustomerGarage.tsx:40,49` | `Property 'message' does not exist on { ok: true; value }` | Discriminated union narrowing — ссылка на `.message` до проверки `.ok === false`. |
| 🟡 **WRONG TYPE** | `pages/provider/ProviderDemand.tsx:29` | `Type 'string' is not assignable to type '(payload: any) => void'` (×2) | Socket-handler сигнатура поломана. |
| 🟢 **TYPE BLIND SPOT** | `pages/provider/BillingPage.tsx:20-36` | 6 errors: `getBillingProducts/Status/Pressure/Tier/Purchases`, `billingCheckout` not on marketplaceAPI | **Runtime fine** — `Object.assign(marketplaceAPI, {...})` в `api.ts:112-120` мерджит эти методы, но `tsc` не видит. |
| 🟢 **TS WARN** | `pages/inspector/{InspectorSettingsPage, ReportWorkspace}` | `Type ... is not assignable to Record<string, unknown>` | Index signature mismatch; runtime OK. |
| 🟢 **TS WARN** | `components/marketplace/ProviderCard.tsx:54` | `expression is never nullish` | Лишняя `??`-проверка. |

### 2.4. Orphan files
**2 файла:**
- `pages/public/SearchPage.tsx` — нигде не импортится, нет роута. Согласно `App.tsx:189`: `<Route path="/search" element={<Navigate to="/specialists" replace />} />` — IA changed, файл не вычистили.
- `pages/public/LiveForecastMapPage.tsx` — нигде не импортится. Скорее всего экспериментальная страница, забытая.

### 2.5. 🟡 API coupling
```
Web-app total unique path patterns: 120
  ✅ Resolved on backend:  83  (69%)
  ❌ Missing on backend:   37  (31%)
```

**Самое больное — `shared/api-contracts.ts` (40-66):**
```typescript
favorites: { my: '/favorites/my', ... }       // backend имеет /customer/favorites
bookings:  { my: '/bookings/my', ... }        // backend имеет /marketplace/bookings + /customer/bookings/{id}/timeline
quotes:    { my: '/quotes/my', ... }          // backend ВООБЩЕ не имеет /quotes/my (только /quotes/{id}/accept, /trust/reviews/*)
vehicles:  { my: '/vehicles/my', ... }        // backend имеет /customer/vehicles
reviews:   { my: '/reviews/my', ... }         // backend имеет /trust/providers/{id}/reviews + /trust/reviews
```

**Подтверждённый runtime:**
```
GET /api/quotes/my       → 404
GET /api/bookings/my     → 404
GET /api/customer/vehicles → 401 (exists, auth required ✅)
GET /api/zones           → 200 ✅
GET /api/services        → 200 ✅
GET /api/marketplace/providers → 200 ✅
POST /api/parse/car-link → 422 ✅ (validation error — endpoint работает)
POST /api/inspection/report/generate → 422 ✅
```

**Также сломаны на runtime:**
- `/provider/requests/*`, `/provider/current-job`, `/provider/presence/update`
- `/quick-request/*`, `/quick-request/inbox/*`
- `/inspector/exposures/{id}` (claim), `/inspector/jobs/{id}/{on-route|arrived|...}`
- `/marketplace/provider/requests/{id}/{accept|reject}`
- `/realtime`, `/slots/reserve`, `/matching/providers`
- `/admin/*` ссылки внутри web-app/api.ts (line ~120) — это leftover, web-app не должен дёргать /admin

### 2.6. Roles & navigation analysis
✅ `App.tsx` корректно разграничивает 3 shell'a + 1 inspector cabinet через `RequireKind`. Принципалы:
- `CUSTOMER_KINDS = ['customer']`
- `OPERATOR_KINDS = ['inspector', 'provider_owner', 'provider_manager', 'admin']`
- `INSPECTOR_KINDS = ['inspector', 'admin']`

✅ `RoleRedirect` для `/app` маршрутизирует по роли.
✅ Legacy routes `/search`, `/zones` → `<Navigate>` (IA-graceful).
✅ Onboarding (`/provider/onboarding`) намеренно вне shell'а — full-screen.

---

## 3. Сравнение Admin vs Web-app

| Параметр | Admin | Web-app |
|---|---|---|
| Build time | 5.32 s | 6.10 s |
| Bundle size (uncompressed) | 2.0 MB | 1.8 MB |
| Bundle size (gzip) | ~410 kB | ~390 kB |
| Module count | 3184 | 6187 (×2 — Leaflet/i18n/Phosphor стоят дороже) |
| Build warnings | 1 (canary chunk) | 0 |
| TS errors | 22 (2 real) | 17 (~11 real) |
| Backend coupling | **36 % OK** 🔴 | **69 % OK** 🟡 |
| package.json чистоты | leftover CRA deps | clean |
| Orphan files | 1 (intentional) | 2 |
| Frozen guards | — | `@customer-grammar/POLICY.md` alias |
| Shell architecture | 1 layout (Layout.tsx) | 3 shells α |
| ESLint config | inherited @eslint/js | inherited |

**Admin зрел архитектурно** (86 страниц, 5 chunk-groups, lazy everywhere), но **сильно деградировал по API** из-за брошенного NestJS.

**Web-app зрелее доктринально** (shared/domain invariants, customer-grammar firewall, 3-shell split, kind-based guards), и backend-coupling в 2 раза лучше, но 4 контракт-обрыва в типах требуют точечного фикса.

---

## 4. Приоритетный action-list (если решите чинить)

### 🔴 P0 — RUNTIME BREAKAGE
1. **Admin: вычистить `AdminVoiceView` reference** в `SupportChatPage.tsx:469` — иначе страница падает с ReferenceError при открытии.
2. **Web-app: исправить импорт** в `StatusToolbar.tsx:4`:
   ```diff
   - import type { InspectionJob, InspectionJobViewModel } from '@platform/domain/contracts/inspection-job';
   + import type { InspectionJob } from '@platform/domain/contracts/inspection-job';
   + import type { InspectionJobViewModel } from '@platform/domain/state-machines/inspection-job';
   ```
3. **Web-app `api-contracts.ts`: исправить my-пути** на канонические backend-маршруты (`/customer/favorites`, `/marketplace/bookings`, `/customer/vehicles`, `/trust/...`) — это починит ~15-20 endpoints одним патчем.

### 🟡 P1 — DECISION REQUIRED (architectural)
4. **Admin: судьба ~50 broken pages** — выбрать одно из:
   - **A)** Re-enable NestJS (`NESTJS_ENABLED=1` + `cd backend && yarn build`) — единственный быстрый путь.
   - **B)** Domain-by-domain migration оставшихся 139 endpoints во FastAPI (как `app/governance/router.py` в Sprint 21 FINAL). Дорого.
   - **C)** Удалить мёртвые admin-страницы (Automation suite целиком + Demand engine + Market rules + Audit log + Feature flags + Users/Orgs/Bookings/Reviews) — если эти модули backend-стороны не возвращаются.
5. Web-app `RawVehicleDoc.location`, `InspectorExposure`-shape, `IssueSeverity::critical` — каждый требует **прочитать sprint-постмортем frozen** в `/app/memory/` и решить: backport контракта или пофиксить UI.

### 🟢 P2 — HOUSEKEEPING
6. Удалить orphan-files: `web-app/src/pages/public/SearchPage.tsx`, `LiveForecastMapPage.tsx`.
7. Из `admin/package.json` убрать `react-scripts`, `cra-template`, `@craco/craco`, `@babel/plugin-proposal-private-property-in-object` (Vite-only сейчас, deps не используются).
8. Admin: `tsc --noEmit` cleanup (20 unused imports — auto-fixable).
9. Web-app: `BillingPage` — заменить `marketplaceAPI.billing*` на `billingAPI.*` напрямую (убирает 6 TS errors одним движением, runtime тот же).
10. Web-app: `App.tsx:189`, оба orphan-файла → удалить или превратить в честные роуты.

### 🟢 P3 — DEPLOY (если нужны SPA в проде)
11. `dist/` обоих SPA сейчас **есть** (я только что собрал), но `app.static.router` смотрит на `/app/admin/dist` и `/app/web-app/dist` — это уже работает через FastAPI `/api/admin-panel/` и `/api/web-app/` (см. `app/core/config.py:36-37`). После любых правок — `yarn build` снова, restart не нужен (static serving).
12. Supervisor сейчас не управляет ни одним из них (на это аудит E2 уже жаловался). Поскольку оба статичны после билда — не критично.

---

## 5. Что я НЕ делал в этом аудите

- ❌ Не правил ни одной строки кода (только установка deps + build + lint, ничего не коммитил)
- ❌ Не перезапускал backend / expo
- ❌ Не запускал backend-suite тесты
- ❌ Не проверял каждый из 95+85 .tsx файлов построчно на качество — проверял **по контракту** (build / type / coupling). «Каждую строчку» в буквальном смысле для 180 файлов невозможно за разумное время, но любой обрыв, который проявится в build/typecheck/runtime, я бы поймал.

---

## 6. Артефакты этого раунда

- `/app/admin/dist/` собран (2.0 MB, 15 chunks) — готов к serving через `/api/admin-panel/`
- `/app/web-app/dist/` собран (1.8 MB, 19 chunks) — готов к serving через `/api/web-app/`
- `/app/admin/yarn.lock` создан (его не было)
- `/app/web-app/yarn.lock` создан (его не было)
- Этот отчёт: `/app/AUDIT_WEB_2026_05_22.md`

---

## 7. Готов к следующему шагу

Скажите, что чинить — могу:
- **A)** Сделать P0 (3 точечных фикса runtime-обрывов) — 15 минут, ничего архитектурно не трогает
- **B)** Pre-mortem по выбору между NestJS-restore / endpoint-migration / page-deletion для admin
- **C)** Web-app: восстановить корректные api-contracts (P0.3) + починить 3 контракт-обрыва типов (P1.5)
- **D)** Просто оставить отчёт, ничего не править
